# ===========================================
# Mobile App – Environment Variables
# ===========================================
# Copy this file to .env in the mobile-app/ directory.
# Variables prefixed with EXPO_PUBLIC_ are exposed to the Expo runtime.

# ---- Google Maps API Keys ----
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY_ANDROID=your_android_google_maps_api_key
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY_IOS=your_ios_google_maps_api_key
# both variables can be the same if you use the same API key for both platforms

# ---- Microsoft Clarity (development builds only) ----
# Clarity is native-only and does not run inside Expo Go.
EXPO_PUBLIC_CLARITY_PROJECT_ID=your_clarity_project_id

# ---- Calendar setup (optional) ----
# URL for the "Classes" calendar setup instructions video. When set, the app shows a "Watch the instructions video" link when the Classes calendar is not found.
# EXPO_PUBLIC_CALENDAR_INSTRUCTIONS_VIDEO_URL=https://example.com/instructions-video

